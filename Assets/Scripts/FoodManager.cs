using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoodManager : MonoBehaviour
{
    static public int foodTaken;

    // Start is called before the first frame update
    void Start()
    {
        foodTaken = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
